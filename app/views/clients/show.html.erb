<% provide(:title, 'Company Profile') %>
<div class="widget wgreen widget-width">
  <div class="widget-head">
    <div class="pull-left"><%= yield(:title) %></div>
    <div class="clearfix"></div>
  </div>
   <div class="widget-content padd">
<!-- ################### -->
    <div class="table-responsive widget-width ">
      <table class="table table-striped table-bordered table-hover">
        <thead>
          <tr>
            <th>Company Name</th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tbody>
            <tr>
            <td><%= @client.name %></td>
            <td><%= link_to "Edit", edit_client_path(@client), :class => 'btn btn-primary' %></td>
            <td><%= link_to "Back", clients_path, :class => 'btn btn-default' %></td>
            </tr>
        </tbody>
      </table>
    </div>
 <!-- ################### -->
  </div>
</div>