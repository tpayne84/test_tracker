<% provide(:title, 'Group Profile') %>
<div class="widget wgreen widget-width">
  <div class="widget-head">
    <div class="pull-left"><%= yield(:title) %></div>
    <div class="clearfix"></div>
  </div>
  <div class="widget-content padd">
<!-- ################### -->

<table class="table table-striped table-bordered table-hover">
      <thead>
        <tr>
          <th>Group Name</th>
          <th></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
          <tr>
          <td><%= @group.name %></td>
          <td><%= link_to "Edit", edit_group_path(@group), :class => 'btn btn-primary' %></td>
          <td><%= link_to "Back", groups_path, :class => 'btn btn-default' %></td>
          </tr>
      </tbody>
    </table>

 <!-- ################### -->
  </div>
</div>